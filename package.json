{
  "name": "my-viplayer",
  "version": "2.0.0",
  "description": "A cross-platform desktop video player (libmpv playback core) with speed control, trim/marks, and keyboard shortcuts",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "node scripts/build.js --mac --win --linux --publish never",
    "build:win": "node scripts/build.js --win --publish never",
    "build:mac": "node scripts/build.js --mac --publish never",
    "build:linux": "node scripts/build.js --linux --publish never"
  },
  "keywords": ["video", "player", "electron", "media"],
  "author": "Boyan <boyanzpetkov@gmail.com>",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^41.2.2",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.myviplayer.app",
    "productName": "My Video Player",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "index.html",
      "styles.css",
      "renderer.js",
      "src/**/*.js",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "vendor/mpv",
        "to": "mpv",
        "filter": ["**/*", "!.gitkeep"]
      }
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ],
      "signAndEditExecutable": false
    },
    "mac": {
      "target": ["dmg", "zip"],
      "category": "public.app-category.video"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Video"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
